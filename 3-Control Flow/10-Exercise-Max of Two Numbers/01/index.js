let number = maxNumbers(6, 5);
console.log(number);

function maxNumbers(numOne, numTwo) {
  // if (numOne > numTwo) {
  //   return numOne;
  // }
  // else {
  //   return numTwo;
  // }
  return (numOne > numTwo) ? numOne: numTwo;
}
